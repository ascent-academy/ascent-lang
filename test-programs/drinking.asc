args (age: Int);

if (age < 13) {
  "Juice box o'clock."
} else if (age < 18) {
  "Soda's your best friend."
} else if (age < 21) {
  "So close, yet so illegal."
} else if (age < 65) {
  "Cheers, you're legal!"
} else {
  "Wine o'clock, every clock."
}
