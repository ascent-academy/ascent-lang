type Person = { name: String, age: Int };

fix ada = Person{ name: "Ada", age: 30 };
fix older = Person{ name: ada.name, age: ada.age + 1 };

fix greeting = "${ada.name} will be ${older.age} next year";

fix people = [ada, older];

people
