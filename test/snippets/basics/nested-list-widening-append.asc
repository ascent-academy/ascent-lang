# expect: value = [[1.0], [2.0], [3.0]]
# Appending a List<Float> widens the receiver to List<List<Float>>, so its
# existing List<Int> elements are re-coerced element-by-element, not left Int.
[[1], [2]].append([3.0]);
