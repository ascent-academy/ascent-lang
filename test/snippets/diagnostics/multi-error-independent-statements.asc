# expect: errors = T0003, T0009
# Two unrelated statements, each with its own genuine error, both get
# reported in a single pass — typecheck() no longer discards the rest of the
# program on the first failure (agenda/phase5.md Phase 5's payoff).
fix xs = [];
True + 1;
