# expect: errors = N0001
# An undefined slot used as a binary operand reports only its own N0001 —
# the '+' does not also cascade a T0009, since the operand already carries
# the checker-internal Invalid type (agenda/phase5.md).
undefinedVar + 1;
