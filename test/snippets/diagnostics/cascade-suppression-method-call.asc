# expect: errors = N0001, N0001
# Both the receiver and the argument fail independently (two real N0001s,
# one per undefined name) — but neither T0006 ('no such method') nor T0007
# ('wrong arg count') fires on top, since methodCall bails to Invalid before
# running its own checks once it sees an already-Invalid receiver or arg.
undefinedReceiver.foo(undefinedArg);
