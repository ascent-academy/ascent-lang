# expect: errors = N0001, T0005
# The condition failing (N0001) only suppresses T0004 ('must be Bool') —
# it does not stop the then/else branches from being checked against each
# other, so a genuine, independent branch mismatch (T0005) still fires too.
if (undefinedVar) { 1 } else { True };
