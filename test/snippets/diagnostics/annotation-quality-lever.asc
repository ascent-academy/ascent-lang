# expect: errors = N0001
# A written annotation wins as the slot's type even when its init expression
# fails to check — 'x' still becomes Int, so the later 'x + 1' checks
# cleanly instead of also cascading a T0009 from a frozen Invalid type.
fix x: Int = undefinedVar;
x + 1;
