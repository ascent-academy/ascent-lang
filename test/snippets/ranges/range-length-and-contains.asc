# expect: value = [True, True, False]
# .contains() tests membership against the half-open bounds: 0 and 9 are in
# 0..10, but 10 (the exclusive end) is not.
fix r = 0..10;
[r.contains(0), r.contains(9), r.contains(10)];
